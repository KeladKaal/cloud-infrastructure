# Лаба 5 — Хранилища данных в облаке (Block, File, Object: выбор и применение)

## 1. Цель работы

Закрепить на практике знания, полученные в лекции 5 «Хранилища данных в облаке»:

- различать три типа облачных хранилищ — **Block**, **File** и **Object** — и понимать их уровни;
- выбирать и применять сервисы **EBS**, **EFS** и **S3** (и их аналоги у других провайдеров);
- учитывать соотношение производительности и стоимости (IOPS, Throughput, Latency);
- выстраивать грамотную архитектуру хранилищ как микс разных типов;
- применять чек-лист выбора и избегать типичных ошибок.

На выходе вы получаете обоснованную схему хранения данных для условного интернет-магазина и отчёт по проекту.

## 2. Формат выполнения

| **Параметр** | **Значение** |
| --- | --- |
| Режим работы | Индивидуально или в паре (1–2 человека) |
| Сдача | Отчёт в формате Markdown (.md), закоммиченный в git-репозиторий, + файл схемы |

## 3. Исходные данные (условие задачи)

Вы — DevOps-инженер компании **«Интернет-магазин „Книжный мир“»** (специфику и название компании можно было придумать самостоятельно или выбрать из списка в Лабе 1). Заказчик просит спроектировать архитектуру хранения данных для того же веб-приложения, для которого вы уже спроектировали сеть (Лаба 1).

Требования заказчика:

| **Сервис** | **Назначение** | **Особенности хранения** |
| --- | --- | --- |
| Веб-фронтенд | Публичный сайт + статика | Статика (изображения, стили, скрипты) раздаётся через CDN, возможны всплески трафика |
| API-бэкенд | Логика заказов, каталог | Выполняется на виртуальных машинах, нужен быстрый системный диск |
| База данных | Хранение каталога и заказов | Высокая нагрузка, критична предсказуемая задержка (IOPS), нужны снапшоты |
| Общий каталог | Контент для нескольких серверов | Несколько серверов должны видеть один каталог одновременно |
| Бэкапы и архив | Резервные копии на петабайты | Многолетнее хранение с минимальной стоимостью, защита от случайного удаления |

Требования к отказоустойчивости и экономии:

- каждый тип данных хранится в подходящем по классу хранилище — никакой переплаты;
- не должно быть Single Point of Failure: бэкапы и снапшоты должны позволять восстановление в любой зоне;
- статика и архивы должны дешеветь по мере «остывания» данных (жизненный цикл).

## 4. Задание и ход выполнения

### Этап 1. Три типа хранилищ и сравнительный анализ

- Обоснуйте, какой из трёх типов (**Block**, **File**, **Object**) подходит для каждого сервиса из таблицы заказчика, и заполните сравнительную таблицу по трём сервисам AWS — EBS, EFS, S3 (шаблон в разделе «Отчёт»).
- Сформулируйте закономерность: как меняются производительность и стоимость от Block к Object.
- Заполните таблицу аналогов EBS, EFS и S3 у провайдеров Yandex Cloud, VK Cloud, Azure, Google Cloud.

**Контрольная точка 1**: каждому сервису соответствует правильный тип хранилища, в системе нет лишней переплаты.

### Этап 2. Сервисы AWS: EBS, EFS, S3

- Для базы данных выберите тип тома EBS и обоснуйте выбор: **gp3** (базовый), **io2/io1** (provisioned IOPS), **st1/sc1** (дешёвые HDD) — что и когда брать.
- Спланируйте снапшоты томов для аварийного восстановления. Укажите, где хранится снапшот EBS — в зоне доступности или в регионе — и почему это важно для восстановления в любой зоне.
- Для веб-фронтенда настройте объектное хранилище под статику и CDN.
- Для общего каталога выберите файловое хранилище и опишите, как несколько серверов монтируют его одновременно (протокол, общая шара).
- Для бэкапов и архива определите классы хранения и правила жизненного цикла (переход объекта между классами по частоте доступа).

💡 *Подсказка из лекции: «Блок — для скорости, Файл — для общего доступа, Объект — для масштаба и денег».*

**Контрольная точка 2**: БД не лежит в объектном хранилище, общий каталог не сделан на блочном диске, архивы дешевеют по жизненному циклу.

### Этап 3. Классы хранения и жизненный цикл в S3

- Заполните таблицу классов хранения S3 от «горячего» к «холодному» (Standard, Intelligent-Tiering, Standard-IA, Glacier, Glacier Deep Archive): назначение, скорость доступа, стоимость.
- Сформулируйте правило жизненного цикла для бэкапов: через сколько дней объект переносится в Standard-IA, через сколько — в Glacier. Обоснуйте экономию.
- Опишите, как работают S3 Select и Glacier Select (SQL-запросы внутри объекта) и зачем они нужны.
- Включите версионирование для бакета с бэкапами и объясните, от чего оно защищает.

**Контрольная точка 3**: правило жизненного цикла описано корректно, версионирование обосновано.

### Этап 4. Архитектура и безопасность хранилищ

- Составьте итоговую схему хранения данных для «Книжного мира» (блок-схему).
- Пройдитесь по чек-листу из 6 вопросов выбора хранилища и примените его к каждому типу данных.
- Опишите, как обеспечено резервирование: где живут снапшоты EBS, в скольких зонах лежит EFS, как S3 реплицирует объекты между зонами.

**Контрольная точка 4**: у каждого типа данных определён тип и класс хранилища, резервирование обосновано.

### Этап 5. Оформление отчёта

Заполните отчёт по шаблону из Лабы 1.

## 5. Отчёт

Отчет должен включать в себя:

### 5.1. Сравнительная таблица трёх сервисов

| **Параметр** | **EBS (Block)** | **EFS (File)** | **S3 (Object)** |
| --- | --- | --- | --- |
| Уровень | Низкий (блоки) | Средний (файлы + папки) | Высокий (объекты/HTTP) |
| Единица данных | Блок | Файл | Объект (ключ + метаданные) |
| Потребителей | 1 EC2 | N EC2 | Бесконечно (HTTP API) |
| Протокол | NVMe | NFSv4 | HTTPS / S3 API |
| Масштабирование | Вручную (том) | Автоматически | Петабайты+ |
| Стоимость на ГБ | Высокая | Средняя | Низкая |
| Задержка | Минимальная | ~10 мс | Зависит от класса |
| Резерв | Снапшот (в регионе) | Много зон AZ | Репликация в регионе |

### 5.2. Таблица назначения типов хранилищ

| **Сервис заказчика** | **Тип хранилища** | **Сервис (AWS)** | **Класс / тип** | **Обоснование** |
| --- | --- | --- | --- | --- |
| Веб-фронтенд (статика) | Object | S3 | Standard → CDN | Масштаб, дешевизна, CDN |
| Виртуальные машины (API) | Block | EBS | gp3 | Быстрый системный диск |
| База данных | Block | EBS | io2 | Гарантированные IOPS |
| Общий каталог | File | EFS | General Purpose | Несколько серверов, NFS |
| Бэкапы и архив | Object | S3 | Standard-IA / Glacier | Жизненный цикл, экономия |

### 5.3. Таблица аналогов у провайдеров

| **AWS** | **Yandex Cloud** | **VK Cloud** | **Azure** | **Google Cloud** |
| --- | --- | --- | --- | --- |
| EBS (Block) | Compute Disks | Volumes | Managed Disks | Persistent Disk |
| EFS (File) | Filestorage | File storage | Azure Files | Filestore |
| S3 (Object) | Object Storage | Cloud Storage | Blob Storage | Cloud Storage (GCS) |

### 5.4. Таблица классов хранения S3

| **Класс** | **Назначение** | **Скорость доступа** | **Стоимость** |
| --- | --- | --- | --- |
| S3 Standard | Горячие данные, частый доступ | Мгновенная | Самая высокая |
| S3 Intelligent-Tiering | Автоматическое движение между уровнями | Мгновенная | Средняя (динамическая) |
| S3 Standard-IA | Инfредко читаемые данные | Мгновенная (плата за доступ) | Ниже |
| S3 Glacier | Архив | Несколько минут | Низкая |
| S3 Glacier Deep Archive | Самый дешёвый архив | До 12 часов | Минимальная |

### 5.5. Схема хранения данных

Вставьте блок-схему архитектуры: **статика в S3 + CDN**, **VM на EBS gp3**, **БД на EBS io2**, **общий каталог на EFS**, **бэкапы в S3 с жизненными циклами**.

### 5.6. Правило жизненного цикла

Приведите сформулированное правило (например: «не читали 30 дней → Standard-IA; 90 дней → Glacier») и обоснуйте экономию.

### 5.7. Выводы

Что запомнили, какие ошибки избежали, что узнали нового.

## 6. Чек-лист самопроверки (перед сдачей)

- ☐ Каждому типу данных подобран корректный тип хранилища (Block / File / Object).
- ☐ База данных — блочное хранилище с гарантированными IOPS (EBS io2), не в объектном.
- ☐ Статика — объектное хранилище + CDN и корневые диски — EBS gp3.
- ☐ Общий каталог нескольких серверов — файловое хранилище (EFS/NFS).
- ☐ Бэкапы — объектное хранилище с классами Standard-IA/Glacier.
- ☐ Снапшот EBS хранится в регионе (восстановление в любой AZ) — отражено в отчёте.
- ☐ Правило жизненного цикла сформулировано и обоснована экономия.
- ☐ Версионирование бакета с бэкапами включено.
- ☐ Указаны аналоги EBS, EFS, S3 у других провайдеров (Yandex, VK, Azure, Google).
- ☐ Схема хранения данных и таблицы приложены.
- ☐ Ресурсы (если использовалась реальная облачная среда) удалены после работы.

## 7. Контрольные вопросы (по итогам работы)

- Кто строит файловую систему поверх блочного диска — хранилище или ОС? Приведите примеры ФС.
- Почему один блочный диск нельзя отдать сразу нескольким виртуалкам, а файловое хранилище можно?
- Где хранится снапшот EBS — в зоне доступности или в регионе?
- Перечислите классы хранения S3 от горячего к холодному.
- Чем S3 Select и Glacier Select упрощают работу с данными?
- Назовите аналоги EBS, EFS и S3 в Yandex Cloud и VK Cloud.
- Сформулируйте правило выбора хранилища одной фразой.
