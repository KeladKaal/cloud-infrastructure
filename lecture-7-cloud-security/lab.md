# Лаба 7 — Безопасность облачной инфраструктуры

> 👥 **Лаба второго преподавателя.** Ниже — авторский текст задания. Содержание не менялось: убраны только время и место выполнения и длительность этапов — их задаёт расписание, а не задание.
>
> **Нумерация.** У автора это лабораторная работа №3, и в тексте он ссылается на свои номера. Соответствие: №1 → Лаба 3, №2 → Лаба 5, №3 → Лаба 7, №4 → Лаба 9, №5 → Лаба 11.
>
> **Сдача — в Markdown.** На курсе все работы сдаются одинаково: отчёт файлом `.md`, схемы — картинкой или диаграммой Mermaid. Подробнее — в [«Регламенте курса»](../COURSE-RULES.md).

---

## 1. Цель работы

Закрепить на практике знания, полученные в лекции №3 «Безопасность облачной инфраструктуры»:

- применить модель Shared Responsibility (разделение ответственности между провайдером и клиентом);
- настроить управление доступом (IAM): роли, политики, сервисные аккаунты, принцип минимальных привилегий;
- организовать безопасное хранение секретов (Vault / AWS Secrets Manager) с ротацией;
- обеспечить шифрование данных in-transit (TLS) и at-rest (диски, S3, БД) через KMS;
- реализовать многоуровневую сетевую защиту: Security Groups, Network ACL, WAF;
- избежать типичных ошибок, перечисленных в лекции, и задокументировать результат.

На выходе вы получаете защищённую облачную среду для условного сервиса и отчёт по безопасности.

## 2. Формат выполнения

| **Параметр** | **Значение** |
| --- | --- |
| Режим работы | Индивидуально или в паре (2–3 человека) |
| Сдача | Отчёт в формате `.md` + схемы и политики |

## 3. Исходные данные (условие задачи)

Вы — DevOps-инженер компании «Интернет-магазин "Книжный мир"». После развёртывания сетевой архитектуры (лаб. №1) заказчик поручает вам обеспечить безопасность этой облачной среды.

Для проектирования вам доступны следующие компоненты инфраструктуры:

| **Компонент** | **Назначение** | **Требование к безопасности** |
| --- | --- | --- |
| Веб-фронтенд | Публичный сайт магазина | HTTPS (TLS 1.2/1.3), защита от атак L7 (SQLi, XSS) |
| API-бэкенд | Логика заказов, каталог | Доступ только из внутренней сети, роли IAM с мин. правами |
| База данных | Хранение каталога и заказов | Недоступна из интернета, шифрование at-rest, секрет в хранилище |
| Служебные сервисы | Бэкапы, телеметрия, CI/CD | Сервисный аккаунт с ролями, ротация ключей, секреты в менеджере |
| S3-бакет | Хранение статики и артефактов | Шифрование объектов, точечная политика доступа |

Требования заказчика к безопасности:

- Доступ к сервисам — через роли IAM и сервисные аккаунты, без постоянного root-аккаунта в production.
- Все секреты (пароли БД, ключи, токены) хранятся только в менеджере секретов, не в коде.
- Все данные шифруются: in-transit (TLS) и at-rest (диски, S3, БД) через KMS.
- Трафик фильтруется на трёх уровнях: WAF → Network ACL → Security Group.
- Ведётся аудит: логирование действий в IAM, доступа к секретам, использования ключей.

## 4. Задание и ход выполнения

### Этап 1. Модель Shared Responsibility

**Задание 1.1.** Постройте Mermaid-диаграмму модели разделения ответственности. Оформите **две независимые ветки-подграфа**:

- **Провайдер** (Безопасность ОБЛАКА): физическая безопасность дата-центров, гипервизор и виртуализация, железо/сети/базовое ПО, физический периметр.
- **Клиент** (Безопасность В ОБЛАКЕ): данные и их классификация, доступы (IAM, пароли, ключи), настройка Security Groups и NACL, шифрование, конфигурация приложений, патчи ОС виртуальных машин.

Требования: узел внутри subgraph — ["Текст · Текст"]; каждое ребро --- записывайте **отдельной строкой** (цепочки A --- B --- C не поддерживаются).

**Задание 1.2 (письменно).** Определите, где проходит граница ответственности (на уровне гипервизора). Опишите зоны ответственности клиента и провайдера для трёх сервисов: IaaS (EC2), PaaS (RDS), SaaS (Office 365). Ответьте, передаёт ли клиент кому-либо ответственность за свои данные.

**Задание 1.3 (кейс).** Через неправильно настроенную Security Group к базе данных в публичной подсети зашёл злоумышленник. Кто отвечает за утечку — провайдер или клиент? Аргументируйте со ссылкой на модель Shared Responsibility.

**Контрольная точка 1:** диаграмма рендерится без ошибок; граница «гипервизор» и ответственность за данные описаны корректно.

### Этап 2. Управление доступом (IAM)

**Задание 2.1.** Составьте корректную JSON-политику для сервисного аккаунта. Требования:

- разрешить только s3:PutObject и s3:GetObject;
- применить строго к бакету my-backup-bucket;
- ограничить условие диапазоном IP 10.0.0.0/24;
- запретить право на удаление объектов.

Политика должна соответствовать принципу минимальных привилегий.

**Задание 2.2.** Заполните таблицу и опишите разницу между сущностями IAM:

| **Сущность** | **Кто это** | **Как подтверждает личность** |
| --- | --- | --- |
| Пользователь (IAM User) |  |  |
| Сервисный аккаунт |  |  |
| Роль (IAM Role) |  |  |

**Задание 2.3.** Сформулируйте принцип **least privilege** своими словами. Для задачи «CI-серверу нужно публиковать файлы в S3-бакет» приведите «правильный» и «плохой» варианты настройки доступа.

**Задание 2.4.** Перечислите 5 типичных ошибок IAM из лекции. Для каждой укажите последствие и меру предотвращения.

**Контрольная точка 2:** JSON-политика валидна, у сервисного аккаунта нет прав администратора, нет * в ресурсах.

### Этап 3. Управление секретами (Secrets Management)

**Задание 3.1.** Заполните сравнительную таблицу Vault vs AWS Secrets Manager:

| **Критерий** | **HashiCorp Vault** | **AWS Secrets Manager** |
| --- | --- | --- |
| Платформа / привязка к облаку |  |  |
| Динамические секреты (TTL) |  |  |
| Автоматическая ротация |  |  |
| Unseal / самоуправление |  |  |
| Стоимость и администрирование |  |  |
| Интеграция с IAM |  |  |

В выводе укажите, какой инструмент выбрать для чисто AWS-стека, а какой — для мультиоблачной среды.

**Задание 3.2 (чек-лист).** Составьте 5 пунктов чек-листа безопасного обращения с секретами на основе лекции. Подчеркните пункт, запрещающий хранение секретов в коде и git.

**Задание 3.3 (кейс).** Пароль от прод-базы закоммичен в репозиторий и стал публичным. Опишите по шагам действия по минимизации ущерба и предотвращению повторения (ротация, вынос в хранилище, ограничение доступа, аудит).

**Контрольная точка 3:** секрет переехал в менеджер и шифруется; доступ к нему ограничен IAM-политикой.

### Этап 4. Шифрование (in-transit / at-rest, KMS)

**Задание 4.1.** Объясните разницу между шифрованием **in-transit** и **at-rest**. Приведите по два примера для каждого (TLS для трафика; EBS-тома, S3-объекты, бэкапы — для данных в покое).

**Задание 4.2.** Опишите схему **envelope encryption** в KMS (3–4 предложения): как генерируется Data Key, чем он шифруется, где хранится мастер-ключ (CMK) и почему он никогда не покидает KMS/HSM.

**Задание 4.3.** Заполните таблицу «Что и как шифруем»:

| **Что шифруем** | **Чем / как** | **Сервис** |
| --- | --- | --- |
| Трафик (сеть) | TLS/HTTPS | ALB, Route53 |
| Диски виртуальных машин |  |  |
| Объекты в S3 | SSE-S3 / SSE-KMS |  |
| Базы данных |  |  |
| Бэкапы |  |  |

**Задание 4.4.** Перечислите 4 типичные ошибки шифрования из лекции и объясните, чем опасна каждая.

**Контрольная точка 4:** шифрование включено и для трафика (TLS), и для данных в покое (EBS, S3, БД); мастер-ключ в HSM.

### Этап 5. Сетевая защита: SG, NACL, WAF

**Задание 5.1.** Заполните сравнительную таблицу:

| **Свойство** | **Security Group** | **Network ACL** |
| --- | --- | --- |
| Уровень работы |  |  |
| Stateful / Stateless |  |  |
| Тип правил | Allow only | Allow + Deny |
| Порядок оценки правил | не важен | по номеру/приоритету |

**Задание 5.2.** Составьте Mermaid-диаграмму flowchart LR порядка прохождения трафика через три уровня (WAF → NACL → Security Group → Приложение). Подпишите уровень работы каждого элемента (L7 / подсеть / инстанс).

**Задание 5.3 (кейс).** Нужно открыть порт 443 для публичного веб-сервера и порт 22 только для админов из внутренней сети 10.0.1.0/24. Сформулируйте правила Security Group (словесно, в формате из лекции).

**Задание 5.4.** Перечислите 3 основные угрозы уровня приложения, от которых защищает WAF (SQL Injection, XSS и др.), и объясните, почему их нельзя заблокировать обычной Security Group.

**Контрольная точка 5:** созданы три слоя фильтрации, соблюдён принцип минимального доступа; БД не доступна из интернета.

### Этап 6. Оформление отчёта

Заполните отчёт по шаблону (раздел 5), приложите диаграммы и чек-лист самопроверки (раздел 6).

## 5. Отчёт

Оформите отчёт по следующей структуре (представьте как текстовый документ).

### 5.1. Модель Shared Responsibility

Вставьте Mermaid-диаграмму двух веток ответственности и опишите границу «гипервизор».

### 5.2. Таблица подсетей и сетевых фильтров

| **Зона / компонент** | **Подсеть / уровень** | **Тип фильтра** | **Stateful/Stateless** | **Ключевое правило** |
| --- | --- | --- | --- | --- |
| Веб-фронтенд | публичная | Security Group | stateful | allow TCP/443 |
| Админ-доступ | — | Security Group | stateful | allow TCP/22 от 10.0.1.0/24 |
| База данных | data | Security Group + NACL | stateful + stateless | only from API |
| Вся VPC | подсеть | Network ACL | stateless | allow + deny по приоритету |
| Приложение | L7 | WAF | — | блок SQLi / XSS |

### 5.3. Политики IAM

Вставьте итоговый JSON-код политики из этапа 2 и таблицу сущностей IAM.

### 5.4. Схема защиты трафика

Вставьте Mermaid-диаграмму WAF → NACL → Security Group → Приложение.

### 5.5. Секреты и шифрование

Приведите сравнительную таблицу Vault vs Secrets Manager, таблицу «Что и как шифруем», опишите схему envelope encryption.

### 5.6. Выводы

Что запомнили, какие ошибки избежали, что узнали нового.

## 6. Чек-лист самопроверки (перед сдачей)

Пройдитесь по списку и поставьте «галочку» напротив каждого пункта:

- ☐ Mermaid-диаграммы корректны (каждое ребро отдельной строкой, заголовки в ["..."]).
- ☐ Граница ответственности в модели Shared Responsibility определена верно (гипервизор).
- ☐ Политика IAM соблюдает принцип минимальных привилегий (нет *, нет прав Admin).
- ☐ У сервисного аккаунта нет права на удаление объектов.
- ☐ Секреты не хранятся в коде / git, вынесены в Vault или Secrets Manager.
- ☐ Для секретов настроена ротация и ограничен доступ через IAM.
- ☐ Шифрование in-transit (TLS 1.2/1.3) и at-rest (EBS, S3, БД) включено через KMS.
- ☐ База данных не доступна из интернета.
- ☐ Созданы все три слоя защиты: WAF → NACL → Security Group.
- ☐ Security Group и Network ACL разведены по смыслу (stateful vs stateless).
- ☐ Логирование / аудит (IAM, доступ к секретам, использование ключей) описано.
- ☐ Ресурсы (если использовалась реальная облачная среда) удалены после работы.

## 7. Контрольные вопросы (по итогам работы)

Отвечают устно на защите или письменно в конце отчёта.

- На ком «безопасность облака», а на ком «безопасность в облаке»? Где проходит граница в модели Shared Responsibility?
- Что такое политика IAM и в чём суть правила «явного Deny» (deny перекрывает allow)?
- В чём разница между пользователем IAM и сервисным аккаунтом? Когда используют каждый?
- Что означает принцип минимальных привилегий и как он применяется к сервисным аккаунтам?
- Почему нельзя хранить секреты в коде? Какие проблемы решают Vault и Secrets Manager?
- В чём разница между шифрованием in-transit и at-rest? Приведите примеры.
- Как работает envelope encryption в KMS? Где физически хранится мастер-ключ (CMK)?
- Чем Security Group отличается от Network ACL (stateful vs stateless)?
- Что такое WAF и от каких атак уровня приложения он защищает?
- Составьте порядок прохождения трафика через WAF → NACL → Security Group.

## 8. Критерии оценивания

| **Балл** | **Критерий** |
| --- | --- |
| 1 | Корректно построена модель Shared Responsibility (граница — гипервизор) |
| 1 | Правильно составлена политика IAM с принципом минимальных привилегий |
| 1 | Секреты вынесены в хранилище, настроена ротация и ограничен доступ |
| 1 | Шифрование in-transit и at-rest настроено через KMS (envelope encryption) |
| 1 | Реализована многоуровневая сетевая защита (WAF + NACL + SG) |
| 1 | Составлены диаграммы и оформлен отчёт |
| **6** | **Итого максимум** |
